{
  "workflow_id": "rig_full_cleanup",
  "name": "绑定全清理",
  "description": "蒙皮权重清理 → Shape 命名修复 → 场景全清理 → 另存",
  "steps": [
    {
      "step_id": "clean_weights",
      "skill_id": "maya_clean_skinweights",
      "parameters": {}
    },
    {
      "step_id": "fix_shapes",
      "skill_id": "maya_fix_shape_names",
      "parameters": {}
    },
    {
      "step_id": "cleanup",
      "skill_id": "maya_master_cleanup",
      "parameters": {"mode": "fix"}
    },
    {
      "step_id": "save",
      "skill_id": "save_scene",
      "parameters": {
        "save_path": "{{publish.save_path}}"
      }
    }
  ]
}

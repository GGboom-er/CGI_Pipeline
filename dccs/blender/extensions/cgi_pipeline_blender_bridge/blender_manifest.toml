schema_version = "1.0.0"
id = "cgi_pipeline_blender_bridge"
version = "0.1.0"
name = "CGI Pipeline Blender Bridge"
tagline = "Connect the current Blender session to CGI Pipeline"
maintainer = "CGI Pipeline"
type = "add-on"

tags = ["Development"]
blender_version_min = "4.2.0"
license = ["SPDX:MIT"]

[permissions]
network = "Listen on localhost for authenticated CGI Pipeline requests"
files = "Create and read the bridge token"

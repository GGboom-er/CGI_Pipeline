# ── CGI Pipeline v2.0 ──

# ── 运行时产物（禁止提交）──
runtime/
sandbox/
publish/
audit/
logs/
reports/
runs/
backups/
tmp/
ipc/
projects/
assets/
*.lock
!filelock
*.stackdump
*.pdb

# ── 本地草稿 / 外部参考（禁止提交新增内容）──
.agents/task.md
.agents/implementation_plan.md
memory/
md_list*.txt
app/
research/
tools/archive/
tests/archive/experimental/

# ── 备份文件 ──
*.bak_*

# ── DCC 大型二进制（不入库）──
*.ma
*.mb
*.blend
*.blend1
*.abc
*.fbx

# ── 本地环境配置（每台机器不同）──
.env

# ── Redis 本地数据 ──
redis_server/dump.rdb

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
.pytest_cache/

# ── 日志 ──
*.log

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
Thumbs.db
Desktop.ini
.DS_Store

# ── 工具生成产物 ──
# graphify 代码图分析输出，可由工具重跑重建，不进版本树。
graphify-out/

# ── 第三方独立仓（只统一位置，不并入本仓版本树）──
# 上游与用途见 dccs/ue/extensions/UPSTREAM.md；照常 git pull 更新。
# 不用 submodule：db-lyon 要跟随上游而非 pin commit，且避免每次 --recurse-submodules。
dccs/ue/extensions/ue-mcp/

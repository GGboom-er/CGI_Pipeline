# 经验沉淀 (Lessons Learned)

本文件是历史问题索引，不是当前规范来源。早期条目可能包含已废弃字段或旧方案名，例如 `report_content`、`report_sections`、`outputs.result`、`CONVENTION.md`；遇到冲突时以 `docs/ai_startup/02_KNOWLEDGE_BASE.md` 及其链接的权威文档为准。

## 架构与引擎

- [变量解析失败] → [未处理字典嵌套查找与列表索引混用] → [在 _resolve_template_vars 中通过 isinstance 和 isdigit 进行分流校验] → [当处理 {{config.x.y.0}} 这样的路径变量时，对于数字必须先转换为 int 才能去列表中索引]
- [魔法字符串污染] → [在工作流 json 中硬编码了 'cache' 等规范性词汇，切换项目即崩溃] → [所有目标组必须使用 {{config...}} 形式去读取 registry 或 project_config] → [永远不要在 Workflow 定义文件中写入任何特定于项目的节点/组名]
- [零散MD报告无法对齐实际任务] → [各技能各自写.md导致信息碎片化] → [禁止技能自行落盘报告，统一通过receipt['report_content']回传主控拼装] → [报告生成权必须归属调度层，原子技能只生产内容不决定输出形式]
- [审计日志detail截断导致报告乱码] → [chain引擎对detail做500字符截断，外部解析json.loads失败] → [用正则从截断JSON中提取关键字段] → [审计日志是审计用途非数据传输，调用方必须容忍截断并实现优雅降级]
- [后台hold卡住] → [QC失败复用NEEDS_ATTENTION] → [改AUDIT_FAILED] → [批处理只完成或失败]
- [恢复跳过失败段] → [失败前标记完成] → [成功后再mark] → [只记录成功段]
- [报告噪音过多] → [中间产物外露] → [唯一MD+.info] → [人看报告机器看JSON]
- [职责冲突] → [ABC导出和几何采集都尝试写材质数据] → [导出只产ABC，材质只由extract_materials产出] → [一个机器产物只能有一个权威生产者]
- [采集职责漂移] → [asset_info混入贴图职责] → [几何只写_info,材质写_materials] → [采集节点不得跨域生产材料数据]
- [输出契约变胖] → [统计字段塞进outputs] → [计数走summary_count] → [outputs只放路径和下游机器契约]
- [默认输出污染] → [compare_result回退输入目录] → [只从.info推导或ERROR] → [机器中间产物不得默认写源目录]
- [ABC输出污染] → [导出默认写源目录或pub] → [abc_path显式或.info推导] → [导出类节点不得碰发布目录]
- [材质JSON散落] → [extract_materials按源blend推导] → [output_path显式或.info推导] → [材料中间产物也必须进.info]
- [缓存分支干扰] → [旧JSON可能被复用] → [每任务重采] → [沙盒产物不做mtime缓存]
- [采集混入诊断] → [Orig缺失直接门禁] → [空几何交给对比] → [采集skill只输出事实]
- [layer名非法] → [多源超长截断成+more] → [资产名拼接并合法化] → [Maya节点名不得直接用原始报告字符串]

## Worker 与服务管理

- [Maya commandPort 阻塞与静默崩溃] → [在 echoOutput=False 模式下强行通过 socket 发送超长字符串脚本，极易导致端口句柄卡死且无法捕获异常] → [放弃原生 socket 强压，改为使用 commandPort 仅发送单行 Base64 引导脚本，从而拉起独立的 RPyC (Remote Python Call) 服务端接管后续通信] → [必须利用 RPyC 代理机制结合 executeInMainThreadWithResult，彻底解决跨进程大对象回传与多线程安全问题]
- [Worker假活] → [PID存活但队列无心跳] → [PID+active_queues双检] → [提交前心跳异常要重启]
- [心跳误报] → [1秒inspect偶发超时] → [默认5秒窗口] → [服务探测不等于任务超时]
- [代码热更新未生效] → [worker持旧模块缓存] → [重启worker再巡航] → [改运行时代码后必须重启]
- [多Maya误连] → [foreground省略端口会落到默认或首个端口] → [强制显式foreground_port] → [多端口场景禁隐式选择]
- [AI误用Maya直连] → [入口文档仍写自动嗅探/旧maya-live] → [同步MCP说明和skill] → [当前场景必显式端口]
- [原始MCP调用报参错] → [漏FastMCP params外壳] → [文档写明wrapper] → [手测禁平铺]
- [新技能 CHAIN_ABORTED "混合多个DCC类型"] → [长驻 Worker 进程的 skill_registry 快照过期，新技能 get_skill_dcc 返回默认值 'maya'] → [在 tasks.py 三处关键位置注入 _reload_skill_registry()：链DCC校验前、单技能执行前、工作流分段前] → [任何依赖注册表的判断逻辑前，必须先热重载，因为 Worker 可能运行数天]
- [shutdown_all() 无法杀旧Worker] → [只遍历 _managed_procs（空列表，因为 Worker 由其他进程启动）] → [增加 PID 文件扫描逻辑，根据 pidfile 内容 os.kill] → [进程管理不能只依赖内存中的句柄，必须有持久化的 PID 文件作为兜底]
- [DCC启动延迟] → [冷启动耗时5-8s] → [实现WarmWorkerProxy常驻池] → [高频任务用常驻池，50次自动重启]
- [DCC疑难根因定位慢] → [后台maya_exec_code的receipt不可查("审计文件为空")、开大rig易卡、整workflow每轮约2.5分钟，盲试多轮难定位] → [让用户开前台commandPort用maya_exec_code(execution_mode='foreground',sync=True)即时交互式探测，或代码里写探针落盘文件再读] → [DCC疑难优先前台即时exec定位根因，别用慢的后台整跑试错循环；结果不可观测时主动加文件探针]

## 数据安全与路径

- [沙盒路径隔离失败] → [Ai_pub映射代理未校验文件存在] → [移除Ai_pub代理，直接抛出FileNotFoundError并使用runs沙盒] → [沙盒拷贝前必须用.exists()强校验源文件]
- [生产数据安全隐患] → [AI或自动化脚本直接修改或生成文件至X盘服务器] → [管线引擎自动将X盘只做读取源，所有文件统一拷贝至沙盒内闭环执行] → [X盘绝对只读，禁止任何形式的增、删、改、覆盖]
- [.env覆盖默认] → [IPC_TIMEOUT_SEC仍为1800] → [同步改0] → [默认修复查.env]
- [中间产物散落] → [workflow未显式传info_dir] → [统一写沙盒.info] → [JSON/ABC只进.info]
- [沙盒名噪音] → [task_id混入用户路径] → [日期时间_资产名] → [task_id只进manifest]
- [根目录JSON复现] → [收尾把JSON当报告] → [reports只收人读报告] → [机器产物只进outputs/.info]

## 对比与验证

- [对比误报顶点偏移] → [JSON精度与阈值不匹配] → [统一全链路为4位小数] → [序列化精度须与对比算法阈值对齐]
- [对比误报差异导致FAIL] → [绑定中合法的Live BlendShape目标体被认定为源数据中缺失的资产] → [在对比引擎中直接过滤以 _live_ 为标志的合法驱动体] → [管线工具在进行资产对齐验证时，必须能够原生地识别并豁免DCC特有的合法过程节点]
- [预对比重复] → [sync内重算] → [compare_result驱动] → [节点间传决策数据]
- [验证误判失败] → [把ORIG_INJECT算阻断] → [看4去向] → [post验证只拦真差异]
- [拼装漂移] → [sync重算对比] → [前置compare_result] → [拼装不独立对比]
- [Maya采集复用] → [core禁DCC] → [放dccs/maya] → [DCC API不进core]
- [新建mesh空几何] → [无历史不产Orig] → [sync补标准Orig] → [采集器不兜底]
- [sync难单测] → [Maya执行和契约混写] → [纯Python契约层] → [DCC大函数外置可测边界]
- [根组漏配] → [只传首根] → [候选根解析] → [geom_roots全传]
- [Orig误判] → [按名称判断] → [official+tweak+连接] → [图关系优先]
- [报告难读] → [英文枚举外露] → [配置映射] → [人读中文]
- [报告过薄] → [只写验收摘要] → [audit渲染明细] → [唯一MD含步骤]
- [运行中报告缺失] → [只在收尾读audit重建] → [调度层实时upsert REPORT.md] → [报告生命周期必须挂在step事件上]
- [复杂报告不可折叠] → [长Markdown混成一块] → [receipt.report_sections分章] → [复杂skill优先返回结构化事实]
- [报告块外露] → [HTML和旧marker在查看器显示] → [普通Markdown+隐藏引用marker] → [报告渲染不依赖HTML折叠]
- [报告重复] → [sections与旧content同时渲染] → [结构化优先屏蔽旧内容] → [同一事实只出现一次]
- [跨段报告覆盖] → [固定block只存本段记录] → [REPORT内隐藏数据合并] → [跨DCC上下文要累积]
- [入口硬编码] → [巡航脚本写死tex/rig路径] → [resolve_asset_files节点化解析] → [workflow只连outputs不写路径]
- [旧geo残留] → [sync保留审核] → [独立清退] → [拼装不删旧根]
- [层级QC过宽] → [误查辅助mesh] → [只查流程根] → [QC边界跟执行依赖一致]
- [层级修复失败] → [顶层旧资产组被锁] → [先解锁层级再删] → [清理散落根必须处理lockNode]
- [修复目标漂移] → [fix自行扫描导致与check不一致] → [fix消费check_result] → [修复节点不得二次决策目标]
- [节点传参断档] → [只传路径需再读盘] → [直传output.compare_result] → [同段节点可传机器字典]
- [旧geo根被删] → [当extra_top_nodes清理] → [先改Group再迁RIG_geo] → [旧绑定根只归一不删除]
- [RIG_geo匹配丢失] → [根RIG_也被剥掉] → [保根剥子级] → [DAG映射区分根和子节点]
- [层级误阻断] → [额外非空顶层当失败] → [输出风险不默认阻断] → [绑定辅助根默认保留]
- [层级回归难发现] → [只靠专项测试] → [并入总门禁] → [关键 workflow 契约进 verify]
- [绑定替换sync崩"Destination is locked"] → [发布rig把renderPartition/defaultTextureList1等默认着色节点用lockNode -lockUnpublished锁成locked container，sync重建mesh后新建SG/纹理自动连入其multi属性时崩；lockUnpublished与普通lock独立，cmds.setAttr/OpenMaya isLocked都清不掉] → [sync建着色前调_ensure_shading_nodes_unlocked：命名默认着色节点集清lockUnpublished+命中即全扫兜底] → [报"locked container"但场景无container节点时查lockUnpublished而非lock；清它只能用lockNode(node, lockUnpublished=False)]

## 技能开发

- [库可用性误判] → [只看资料未实跑] → [导入加最小运算验证] → [依赖能力以实跑为准]
- [Maya相对保存失败] → [project重定向] → [保存前resolve绝对路径] → [DCC写文件禁相对路径]
- [查询不等于闭环] → [JSON没验证skinCluster] → [写回副本重开比对] → [权重迁移必须复验DCC]
- [直接ROM无效] → [joint被rig网络覆盖] → [用合成LBS验证权重] → [ROM先确认真实运动]
- [合并口腔mesh误归单源] → [旧牙龈牙齿分拆] → [组合owner加分] → [merge目标优先识别composite]
- [owner只看距离] → [口腔眼部近邻歧义] → [语义几何top joint合评] → [归属必须多证据]
- [权重只看ROM不足] → [rig控制链可能覆盖joint] → [权重向量云补验] → [skin验证要数据+运动双轨]
- [runs脚本找不到core] → [sys.path只有脚本目录] → [注入仓库根] → [沙盒脚本显式定位repo]
- [winding扰动复测超时] → [逐点重复算layer] → [缓存support domain] → [layer查询禁交互式重算]
- [腰带外推敏感] → [近层几何无barrier] → [加owner/layer约束] → [belt/cloth需单独诊断]
- [全局场近层串扰] → [候选源过宽] → [owner过滤局部field] → [敏感附件先限域再采样]
- [网上依据未落地] → [只引用不验证] → [资料后跑批量实测] → [研究结论必须绑定本地数据]
- [原型难复用] → [runs脚本未入registry] → [固化为skill] → [验证过的流程要产品化]
- [重复诊断覆盖] → [.info固定文件名] → [写前备份] → [机器产物也要防覆盖]
- [MFnMesh建模报错] → [parent当位置参数] → [显式parent关键字] → [Maya API按官方签名调用]
- [多语义merge失败] → [per-mesh单owner] → [patch级归属] → [合并mesh先分component]
- [split低置信但权重准] → [部分body bbox分数低] → [分裂场景放宽置信] → [confidence需区分局部片段]
- [合并mesh串权重] → [整mesh单owner] → [component级owner] → [断开shell必须分开继承]
- [局部patch误判] → [对称Chamfer惩罚大源] → [单向距离] → [subset匹配看target到source]
- [焊接mesh仍失败] → [component拆不开] → [patch级归属] → [连续多语义面需再切patch]
- [桥接测试不过] → [连通边跨语义] → [边界检测] → [owner不能只看component]
- [噪声patch污染] → [近邻误吸小远片] → [小且远吸收] → [patch需二次过滤]
- [IDW单邻居崩溃] → [k=1返回一维] → [升维处理] → [KDTree k=1必测]
- [BS资产先验不足] → [未盘点直接BS] → [先inventory] → [BS开发先查真实target]
- [LiveBS误降级] → [同时有点位和连接] → [总查inputGeomTarget] → [live绑定不能当静态delta]
- [MFnComponentList误用] → [API2不支持下标] → [用get(index)] → [DCC API先查官方再实跑]
- [BS可视化倍增] → [用targetMesh建live链] → [直写inputPointsTarget] → [验收必须测0到1位移]
- [BS节点叠加] → [底板已有BS权重1] → [先清旧BS再写] → [验收必须查history节点数]
- [LiveBS假通过] → [只写静态delta] → [连接inputGeomTarget] → [动态BS验收必须查live链]
- [BS复验基准错] → [用已变形mesh采样] → [先置0再采样] → [0到1差值必须基于neutral]
- [LiveBS权重半污染] → [active行未清body权重] → [先归零再写live权重] → [验收要查joint质量]
- [内部BS漏复刻] → [只迁外层target] → [live target补内部BS] → [动态BS要验内外层]
- [内部BS口径偏差] → [按9238 live点验最终体] → [按源body最终delta] → [BS验收看最终输出]
- [LiveBS漏姿态] → [只测alias] → [测ctrl=25] → [动态BS验控制器]
- [嘴唇串权] → [近层空间歧义] → [姿态残差] → [mouth需ROM校准]
- [口腔新增静态] → [source-only未配rig] → [配live skin] → [牙舌不能只搬mesh]
- [上下唇互串] → [空间映射穿插] → [ROM软标签交换] → [口腔权重需运动证据]
- [牙龈牙齿合并] → [target被当单独牙齿] → [teeth+gum composite owner] → [口腔merged mesh先拆语义]
- [唇角残余混权] → [静态owner漏判] → [用Jaw ROM运动判lower] → [屏障规则需接pose evidence]
- [Maya端口读超时] → [长修复仍在DCC执行] → [先轮询产物] → [socket超时不等于任务失败]
- [Live源mesh拿错] → [用RIG_body修face live] → [改用M_Head_base] → [动态BS target按真实source传skin]
- [嘴部距离假通过] → [近层距离小但运动反] → [测motion vector] → [口腔权重验收必须测运动]
- [JawUp误归上唇] → [support joint混入UpLip] → [严格按UpLip/LoLip/Jaw分家] → [语义标签先审joint族]
- [近邻运动仍粘] → [未验上下唇owner] → [测strict lip label] → [嘴唇验收要权重语义]
- [PowerShell逗号拆参] → [未加引号] → [参数整体加引号] → [列表参数命令行需引用]
- [跨库数组不兼容] → [TrackedArray被拒] → [先转contiguous ndarray] → [外部库入参先规整]
- [PyTorch3D失效] → [torch版本抢先] → [对齐2.10] → [二进制库版本同源]
- [OpenVDB失效] → [Open3D先加载DLL] → [VDB先导入] → [体积步隔离进程]
- [conda求解异常] → [历史build规格误解] → [核心conda+wheel补齐] → [混装分层验证]
- [巡航入口启动失败] → [Start-Process截断python -c] → [补tools包装脚本] → [长任务入口用脚本文件]
- [环境误跑] → [PATH命中系统Python/conda输出编码] → [直调env python] → [pipeline命令用绝对解释器]
- [CLI单技能失效] → [WarmWorkerProxy无start] → [start做幂等no-op] → [工厂返回对象接口要稳定]
- [技能文件夹自包含] → [分发部署需打包文件夹，.py散落根目录不便管理] → [adapter改为skills.{id}.{id}加载+__init__.py兼容外部import] → [每个技能=一个文件夹({id}.py+SKILL.md+__init__.py)，可独立打包]
- [自动巡航测试参数透传失效] → [调用 blender_export_abc 时错将参数名设为 export_path，导致内部降级使用 source_path 推导] → [检查并对齐工作流载荷与原子技能的 parameters 键名] → [组装自动化执行链时，必须严格校验上下游节点约定的输入/输出字段名，避免静默降级]
- [工作流引用不存在的skill_id] → [旧工作流JSON用短名(master_cleanup)而非全名(maya_master_cleanup)] → [全量审计修复为正确的skill_id] → [工作流JSON中的skill_id必须与SKILL.md中声明的skill_id完全一致，不能省略前缀]
- [技能审计失配] → [回执仍用旧短名和自定义路径key] → [统一skill_id与output_path/report_path] → [改skill输出前必须同步workflow模板]
- [技能契约漂移] → [frontmatter旧类型] → [统一枚举] → [交付前跑扫描]
- [模板未解析] → [replace回归成非法路径] → [加扫描] → [跨段模板进门禁]
- [多占位符误吞] → [正则跨模板匹配] → [禁止匹配花括号] → [同串多模板必测]
- [节点输出漂移] → [业务字段塞outputs顶层] → [只准三键] → [特殊参数走outputs.result]
- [BS误传] → [全history扫到target上游] → [按output链确认归属] → [只传直接变形当前mesh的BS]

## 编码规范

- [API 废弃导致依赖崩溃] → [移除核心路径保护逻辑时未连带清理子模块中对 suggest_ai_publish_path 的 import] → [全局搜索并移除废弃函数的引用] → [废弃或重构核心 API 时，必须执行全仓库 grep 搜索，确保无孤立的跨模块调用残余]
- [Python 语法报错 (SyntaxError)] → [在 logger.info 中直接手敲换行符导致未闭合字符串] → [使用显式的转义字符 \n 或三引号] → [严禁在单行函数调用内混入原生回车换行]
- [测试契约漂移] → [脚本读取旧uvsets字段] → [改测u/v/uv_indices] → [测试字段跟asset_info契约同步]
- [旧测试误导] → [废弃脚本仍在根tests] → [归档legacy_manual] → [当前入口写README]

## 文档治理

- [论文路线误当实现] → [外部依据和本地能力混写] → [文档分理论/实测/待产品化] → [研究结论必须标明落地状态]
- [文档权威漂移] → [历史方案与现契约并存] → [先分权威/专项/历史/归档] → [整理前必须定唯一真相源]
- [文档迁移断链] → [移动后索引仍指旧路径] → [迁移后跑引用扫描] → [文档重组必须同步索引]
- [专项文档过期] → [重构后旧链路残留] → [重写权威段落] → [bak归档不当现行规范]
- [归档误用] → [历史文档混在主目录] → [移入archive并写索引] → [主docs只放现行契约]
- [报告契约膨胀] → [多套展示字段混用] → [只渲染标准记录] → [报告不二次理解]
- [报告仍像日志] → [输入输出JSON直铺] → [按step块+details渲染] → [报告必须先看链路再展开明细]
- [报告折叠失效] → [details混用MD表格] → [移除对比明细表] → [报告先看Output]
- [Output重复Details] → [嵌入result被展平] → [只展平单独result] → [明细只进Details]
- [报告明细依赖兼容字段] → [report_sections绕过output契约] → [明细迁入output字段] → [报告只渲染output]
- [报告首屏过长] → [step正文外露] → [step整体details] → [默认只露执行摘要]
- [Skill契约漂移] → [复制旧字段] → [唯一规范入口] → [新增只设计input/output]
- [失败回执丢明细] → [调度层覆盖JSON] → [保留receipt只补error] → [链失败不得改detail]
- [子链报告重复] → [子链先finalize] → [父workflow统一收尾] → [子段只upsert step]
- [CLI轮询早停] → [中间态当终态] → [终态白名单] → [wait只认明确终态]
- [上下文恢复丢规则] → [规范散在流水账] → [知识库最小入口] → [恢复先读KNOWLEDGE_BASE]
- [旧脚本误导实现] → [archive暴露散装样例] → [备份后移出索引] → [归档脚本不当规范]
- [测试靠记忆] → [门禁清单未更新] → [tests README锁定] → [新增规范必须补测试入口]
- [旧报告误报FAIL] → [total_issues含ORIG] → [按blocking判断] → [用户状态只看阻断]
- [规范多头] → [指南与总规范重复] → [唯一CONVENTION] → [其他文档只引用]
- [规则仍散] → [文档不是触发入口] → [收敛到构建skill] → [规则必须可被调用]
- [跳转页干扰] → [入口多但无正文] → [只留规范正文] → [规范文件必须确定]
- [PAIRED层名撞mesh] → [单源沿用mesh名] → [统一资产名_Layer] → [displayLayer不复用transform名]
- [文档重复反复改] → [运行时/skill/专项规则混写] → [按所有权拆权威文档] → [新增规则先选唯一归属]
- [开工不读规范] → [只靠聊天记忆] → [任务前先读知识库] → [需求场景以KB为准]
- [前台Maya误绕MCP] → [直接连commandPort] → [FastMCP call_tool] → [Maya操作只走MCP]
- [资产名跑源文件workflow] → [缺source_resolution] → [声明model blend解析] → [无resolve节点必须声明源解析]
- [跨DCC分段误开文件] → [后段继承全局source] → [仅首段默认继承] → [后段source_path必须显式]
- [ABC层级构建失败] → [短DAG路径parent] → [全程使用long path] → [创建DAG后立即规整长路径]
- [空Maya场景保存失败] → [save_scene强依赖场景名] → [沙盒save_path可保存] → [构建型workflow按save_path落盘]
- [空source污染场景] → [Warm Pool沿用内存场景] → [空路径强制new scene] → [空source必须显式清场]
- [经验散落] → [只写单个SKILL.md] → [回归build_pipeline_skill] → [跨skill规则必须中央化]
- [会话记忆丢失] → [无固定启动入口] → [新增memory目录] → [所有AI开工先读memory]
- [ABC顶层泄漏] → [读取后仍用原dag_path] → [mesh key使用display_path] → [ABC只是容器根]
- [AO误作Color] → [Base Color追踪未判语义] → [非color贴图降级solid] → [AO不得连接color]
- [近层最近点串权] → [欧氏近但拓扑远] → [权重岛+测地传播] → [候选先按支撑域限域]
- [拓扑标签过扩散] → [ROI含面颊头部] → [先lip/jaw限域] → [传播域不能一锅端]
- [语义切换误写风险] → [标签对但motion差] → [加motion gate] → [写权重前必须证伪退化]
- [参数扫不动大面] → [安全候选仅13点] → [查BS/拓扑表达] → [别靠调参掩盖欠约束]
- [同族高误差误判] → [最近源样本错] → [查weight delta] → [样本匹配好不等于写权重]
- [写入链路误判] → [只验skinCluster] → [测final body delta] → [写权重必须验最终输出]
- [节点关闭误判] → [只看视口] → [查envelope链路] → [先证节点状态]
- [整体口型误判] → [局部截图外推] → [测lip gap] → [全局过关查局部]
- [扩大skin冲动] → [risky被motion反证] → [先residual审计] → [负improve禁止写]
- [复刻目标跑偏] → [把final当target] → [锁MHead到live] → [final仅作验证]
- [Corrective误写] → [risky残差高] → [先验motion gate] → [被反证点禁止出BS]
- [新手入口缺失] → [框架散落] → [建PROJECT_FOUNDATION] → [开工配置必读]
- [必读路径分散] → [入口跨目录] → [收拢ai_startup] → [必读包固定目录]
- [旧入口冗余] → [仅剩跳转文本] → [删除旧路径] → [规则只写ai_startup]
- [Maya scipy假通过] → [只测import scipy] → [测scipy.spatial] → [前台重计算先测子模块]
- [BS嘴唇串形] → [BS仍用裸最近点] → [delta走语义支持域] → [skin/BS共用映射约束]
- [离线proxy误导] → [候选写入后变差] → [实际写回验证] → [skin候选必须闭环测]
- [skin宽写冲动] → [113点76点退化] → [实际收益门控] → [只保留正收益点]
- [LBS反求前提] → [不验矩阵会错算] → [先重建skin输出] → [max微误差才可反求]
- [单姿态反求风险] → [只拟合Jaw25] → [多姿态回归] → [防止控制器角度过拟合]
- [9340单姿态通过] → [低角度新增碰撞] → [多姿态门控] → [Jaw25通过不等于区间通过]
- [v038低角度碰撞] → [四点误差换接触] → [局部回滚验证] → [接触安全优先于max误差]
- [射线方案误用] → [只判穿插不判owner] → [作penalty门控] → [ray不能单独传权重]
- [逐点回滚无解] → [面片连贯性缺失] → [patch级约束求解] → [视觉坏形要整片优化]
- [smooth-only不足] → [视觉目标未入优化] → [候选需visual gate] → [离线不过不写Maya]
- [离线LBS过乐观] → [实际图含动态BS] → [Maya回读验收] → [实际图优先于代理]
- [Maya scipy冲突] → [conda scipy混Maya numpy] → [Maya只导点] → [评分放conda环境]
- [旧矩阵误导] → [场景已被写回改变] → [重采fresh target] → [当前场景用当前probe]
- [source输入误采] → [envelope0非真实输入] → [用已验证inputGeometry] → [source/target采样分开验]
- [反求权重跑偏] → [单姿态追motion] → [加source权重先验] → [权重不能远离语义]
- [手绘不穿插] → [不等于source真值] → [分离视觉目标和pose误差] → [穿插修复需多目标验收]
- [连续面误判] → [法线先于拓扑] → [先测mesh graph距离] → [射线/法线只作风险证据]
- [硬禁唇链变差] → [owner约束过硬] → [回归source-prior先验] → [禁influence前必须验pose]
- [离线脚本复用旧probe] → [忽略v027结论] → [统一fresh target matrix] → [当前嘴唇候选禁用旧target矩阵]
- [Maya读NPZ失败] → [numpy版本pickle不兼容] → [转纯JSON] → [跨Python环境禁传npz对象数组]
- [RPyC端口复用失败] → [单值server阻止新端口] → [按端口缓存server] → [前台多端口不能用单例]
- [离线source先验误导] → [未进实际依赖图] → [actual graph验收] → [视觉风险变差禁止通过]
- [上唇掉下唇] → [source-prior跨唇flip无门控] → [actual行审计] → [上下唇dominant flip默认禁写]
- [候选current过期] → [fresh_matrix不是写前状态] → [写前回读before_rows] → [候选门控禁用stale probe]
- [映射不可追责] → [候选未存source索引] → [保存provenance] → [点级映射必须可审计]
- [source LBS重建大错] → [bindPreMatrix物理索引错用] → [用indexForInfluenceObject] → [skin矩阵必须按logical index]
- [clean回归无写点] → [旧候选由污染数据驱动] → [回干净基线] → [皮肤写入必须从clean probe开始]
- [lip全变head] → [family匹配完整DAG祖先] → [只匹配leaf joint] → [owner标签禁用完整路径token]
- [单点被最近点带偏] → [缺少patch拓扑投票] → [source unary+MRF] → [单点异常要解释归属]
- [近层owner不稳] → [ICM局部极小] → [GraphCut扩张] → [唇/指owner用图割]
- [ROI热测地崩溃] → [子网格有孤点] → [compact引用点] → [heat前清理mesh]
- [测地过度引入] → [把距离当owner] → [只作prior] → [owner仍走图割]
- [target权重误导] → [9194当前为lower] → [查owner和source行] → [写前比三层family]
- [权重写入假阴性] → [skinCluster envelope为0] → [先查deformer状态] → [actual验收必须启用链路]
- [unsafe点扩写风险] → [高误差多为低置信] → [细分拒因再优化] → [unsafe_base禁止直接写]
- [已有collision放行] → [motion改善但接触变差] → [actual接触回滚] → [新增collision必须剔除]
- [单点高误差诱惑] → [9346改善会造穿插] → [保留回滚] → [motion低于contact门禁]
- [单点回滚误判] → [9346需patch平滑] → [局部多目标求解] → [rollback点可用patch重审]
- [剩余高误差追逐] → [9175补权会增collision] → [actual门控拒绝] → [max error不能压过接触安全]
- [回滚过于保守] → [好坏二选一] → [多姿态alpha] → [先搜最大安全比例]
- [视觉指标轻微回退] → [alpha只看接触] → [加入visual penalty] → [下一版门控覆盖形面]
- [unresolved离线通过] → [多姿态实际增collision] → [实采后筛点] → [离线source-prior不能直接推荐]
- [局部点可降误差] → [低角度或visual变差] → [alpha/成对搜索拒绝] → [9175类点禁止硬写skin]
- [低误差仍错权] → [current权重自证] → [加内蕴描述子] → [用户点先重审source]
- [近层点源重复] → [欧氏近邻陷阱] → [测地landmark对应] → [唇/指先查intrinsic]
- [自动找近层错权] → [写后体检太晚] → [先扫intrinsic冲突] → [owner前置再写skin]
- [阈值漏边界点] → [单硬门限卡0.0004] → [hard+borderline分层] → [低置信进复核]
- [法线单独判owner] → [近层同向/过渡混杂] → [只作penalty] → [owner主证据仍是intrinsic]
- [传权前看不出风险] → [只看最终穿帮] → [建preflight风险set] → [skin写入前先看mapping set]
- [弱匹配错点漏检] → [descriptor距离硬卡0.18] → [加margin+局部环证据] → [风险识别不能只靠距离]
- [人工点号污染规则] → [验收样本进生产逻辑] → [自动扫描另写v052] → [点号只能作回归验证]
- [风险点直接全写] → [无actual门控会复发] → [v053逐点alpha] → [自动写前必须多姿态验收]
- [整身接头部源] → [远离source天然低置信] → [写诊断集] → [低置信不等于嘴部失败]
- [权重写完未验] → [只看roundtrip不看形变] → [Jaw25实测] → [交付必须验证envelope和运动]
- [expected过拟合] → [自生成pose当真值] → [外部motion门控] → [只采纳正收益点]
- [LBS评估离谱] → [einsum矩阵轴写错] → [对齐已验证基线] → [新指标先校准旧指标]
- [合成多姿态炸裂] → [单骨矩阵不等于rig层级] → [采真实ROM] → [产业级反求禁用伪pose]
- [FK反求收益小] → [表情含SDK/BS驱动] → [FK只作skin基底] → [表情需DG或residual]
- [对照体复制失败] → [parent后长路径失效] → [用parent返回名] → [DAG改动后重解析]
- [显示辅助中断写权] → [override属性锁定] → [显示失败可跳过] → [非核心辅助不阻断]
- [v057未改嘴角] → [FK门控未覆盖局部] → [查raw候选] → [先试小patch]
- [raw局部有效] → [全局门控排除] → [独立试验体] → [诊断候选不混生产]
- [raw嘴角更差] → [expected_motion非真值] → [判定REJECT] → [形面目标优先]
- [权重差异全召回] → [松阈值误报多] → [只作风险探针] → [修权仍需对应/验收]
- [嘴角上下内外混乱] → [未建局部语义坐标] → [先求chart再映射] → [禁止点级补丁]
- [权重可作UV] → [需source真值语义] → [chart owner先验] → [错误target权重只作风险]
- [低置信误当风险] → [宽ROI含非嘴唇] → [strict冲突单独选] → [broad诊断不得默认选]
- [语义冲突误当穿帮] → [owner低置信也进strict] → [要求source provenance] → [strict必须可操作]
- [旧NPZ当base] → [当前场景权重已变] → [Maya实时回读base] → [权重反求先锁数据源]
- [全矩阵prune] → [未接受点也被修改] → [只改accepted行] → [changed必须等于accepted]
- [full-strength写入穿面] → [motion目标压过形面] → [alpha+visual gate回归] → [actual graph不过不推荐]
- [高斯平滑诱人] → [局部降误差但全唇fail升高] → [owner拓扑约束且可回滚] → [平滑不能默认替代对应]
- [非嘴唇邻点抖动] → [局部权重行不连续] → [same-owner拓扑平滑] → [噪声用邻接位移回归验收]
- [静态快照误判] → [snapshot无skinCluster] → [原始目标未写入] → [交付必须查目标skinCluster]
- [绑定姿态污染] → [非零控制器下建skinCluster] → [归零后绑定] → [写权前先锁neutral]
- [全局骨骼复刻差] → [权重写入闭合但位移场不闭合] → [全influence反求] → [transfer后必须逐骨骼验位移]
- [只测旋转漏诊] → [位移/缩放通道未覆盖] → [R/T/S全通道采样] → [骨骼复刻验收不能只测rotate]
- [映射误当反求] → [只插值source权重] → [建位移bank再拟合] → [transfer和inverse分开验收]
- [反求拟合跑偏] → [expected位移不可信] → [先证明correspondence] → [求解器不能修错误对应]
- [对照体绑定偏移] → [先移位再bind] → [原位bind后移位] → [视觉对照不能污染bind]
- [raw反求眼皮炸] → [眼皮映射到嘴唇] → [语义一致性门] → [冲突点禁止反求接管]
- [A复刻复用旧map] → [body2对应关系污染A] → [重建A专用map] → [不同target禁复用correspondence]
- [头源套整身A] → [远域无source支持] → [support/fallback分域] → [远域不算头部验收]
- [KNN嘴层炸] → [邻近层混权] → [actual graph对照] → [别把KNN当默认]
- [高斯平滑退化] → [平滑跨弱边界] → [只作候选] → [默认需姿态回归]
- [同面权重断崖] → [最近点逐点错分] → [GraphCut owner+边界平滑] → [写前必须测face L1]
- [视觉全不合格] → [face L1局部过关不代表形面过关] → [Maya DG多指标定位] → [改算法前先建失败set]
- [实验链污染] → [旧场景旧NPZ混用] → [归档重启v082] → [新轮只读test.ma]
- [整身A传头源] → [身体远离M_Head_base支撑域] → [unsupported单独标记] → [低置信默认不等于可写]
- [只加candidate_k无效] → [最近距离排序没变] → [引入normal/semantic energy] → [改映射要改候选评分]
- [低置信减少但形变差] → [诊断指标不等于权重目标] → [Jaw motion-delta验收] → [映射参数必须过DG]
- [不同provenance比误差] → [评测口径不一致] → [同canonical验收] → [候选对比必须同真值]
- [hybrid收益很小] → [只修局部motion] → [转局部约束反求] → [小幅p95不算完成]
- [拓扑补洞炸嘴] → [权重语义被平均抹平] → [加语义约束反求] → [无约束inpaint禁用]
- [basis索引越界] → [roi,influence,xyz误当xyz,influence] → [求解前转置] → [反求脚本先验shape]
- [v084单姿态有效] → [只验Jaw25] → [补多姿态DG] → [单姿态通过不等于完成]
- [v084压Jaw退Cheek] → [单目标反求偏置] → [多姿态门控] → [Jaw收益需Cheek验收]
- [整行回退过保守] → [丢失Jaw收益] → [逐点alpha] → [v085只作安全对照]
- [逐点alpha有效] → [整行回退丢收益] → [pose向量搜alpha] → [回归点清零后再看Jaw收益]
- [Jaw目标跑偏] → [单姿态替代全权重] → [全influence R/T/S] → [骨骼矩阵整体验收]
- [raw全局更优] → [大面积改权风险高] → [语义置信门控] → [raw只作诊断]
- [NPZ跨环境失败] → [NumPy object pickle不兼容] → [字符串存Unicode] → [Maya读NPZ禁object]
- [离线略优] → [真实DG多姿态回归] → [actual-DG-in-loop] → [离线指标不能定版]
- [小改动仍退化] → [局部收益跨控制器不稳] → [多姿态门控] → [28点也要验收]
- [射线证据有用] → [高lift但低recall] → [作risk penalty] → [不能单独删映射]
- [近层front命中] → [能预测部分回归] → [并入visibility gate] → [仍需DG验收]
- [连续场反求超时] → [逐joint/channel重复求场] → [场作先验] → [生产链路拆阶段]
- [field_prior炸裂] → [连续权重场非最终权重] → [只作诊断先验] → [写回必须DG门控]
- [actual_safe仍回归] → [只看raw历史误差不够] → [叠加visibility/语义/拓扑] → [最终候选联合门控]
- [局部候选全局误改] → [top-k prune未限mask] → [只裁剪changed行] → [patch候选先验unchanged]
- [候选表面分数爆炸] → [展示偏移进skin bind] → [原位bind后再移位] → [对照offset不得参与绑定]
- [surface收益假阳性] → [只看点位误差] → [边长面积法线同验] → [patch候选必须表面门禁]
- [零回归无体感] → [候选过于保守] → [加最小可见收益] → [不明显改善不算有效]
- [离线patch下降] → [真实DG仍回归] → [actual surface gate] → [patch优化后再投影验收]
- [宽门禁strict退化] → [局部收益跨区泄漏] → [选patchLoose] → [默认看综合指标不看单项]

## MCP 工具暴露

- [ueRBF肩部空间污染] → [RBF输入使用 MicroElbow_R.worldMatrix * Shoulder_R.worldInverseMatrix，但 Shoulder_R 不是 FK/IK blend 后的当前肩部解算空间，且原 ElbowQRotateMMTwist_R.matrixIn[1] 还含有训练空间静态校准 offset] → [UERBFSolver.inputs[0] 改接 MicroElbow_R.worldMatrix * ShoulderXformInverter_R.worldInverseMatrix * 原 ElbowQRotateMMTwist_R.matrixIn[1] 静态矩阵，保留原 ElbowQRotateMMTwist_R 给 twist 链路] → [动画模式 RBF 输入必须取 FK/IK blend 后的解算空间（ShoulderBM/ShoulderXformInverter）并继承训练链路静态 offset；不要用变形 joint parent，也不要只看旋转忽略 matrixIn 静态校准]
- [AI识别工具但不用] → [58个工具上下文膨胀+描述缺场景驱动] → [精简到39个+英文路由决策树+annotations] → [工具数<40且instructions前3行写路由]
- [exec_code双重注册] → [SKILL.md动态注册exec_code+手动注册maya_exec_code] → [exec_code加入排除列表] → [有手动Tool的skill必须排除动态注册]
- [AI绕MCP裸连socket] → [instructions缺"NEVER use raw socket"显式禁止] → [instructions顶部英文决策树+参数描述写"Get from maya_list_foreground_sessions"] → [工具描述必须写"INSTEAD OF"竞品引导]
- [动态工具无annotations] → [mcp.add_tool(fn)不传annotations] → [Tool.from_tool()按前缀推断readonly/destructive] → [动态注册必须带annotations]

## 代码库治理

- [新人看不懂仓库] → [29个顶层目录+4700文件82%是噪声] → [P0清理运行产物+P1归档实验+P2文档拆分] → [核心目录<15个且tests门禁覆盖所有活跃测试]
- [160KB架构文档] → [规范和实验日志混写] → [拆为22KB规范+174KB归档日志] → [architecture/只放规范，archive/放迭代日志]
- [21个测试在门禁外] → [不知道该跑哪些] → [4级门禁P0/P1/P2/DCC+README明确分类] → [新增测试必须选级别入门禁]
- [tools/根目录散落实验脚本] → [13个run_*.py混在生产代码旁] → [全部移入tools/archive/] → [tools/根目录只放README]

## Skill 审查

- [SKILL.md参数与代码不匹配] → [21/44 skill有参数不对齐] → [分4类B/D/A/C批量修复] → [改动skill代码时必须同步SKILL.md参数]
- [间接读取参数误判] → [sync_rig通过sync_contract间接params.get] → [审计工具不能只grep顶层代码] → [复杂skill先查契约层再判断]
- [框架参数混入用户参数] → [asset_name/run_dir/task_id不该暴露为MCP tool参数] → [frontmatter不声明+参数规则章节说明] → [框架参数和用户参数必须区分]
- [PowerShell ConvertFrom-Json报UTF-8中文JSON无效] → [PowerShell默认编码非UTF-8] → [用Python json.load验证JSON] → [Windows上永远用Python做JSON校验]
- [compare/pipeline_compare的output_path被误判为"代码不用"] → [参数经resolve_compare_result_path间接消费] → [先追踪core层调用链再删参数] → [移除参数前必须grep core/层的调用函数]

## AI 协作与决策

- [外部工具出的方案违反本仓硬规则] → [Notes 治理层 AI 用 workflow 审查待办时只读了治理层知识库，没读本仓 AGENTS.md/tasks/todo.md，给出"批量改 45 个 SKILL.md 加 tier 字段"的方案] → [开工前先读本仓 AGENTS.md + docs/ai_startup + tasks/todo.md，发现冲突后改走 build_pipeline_skill 规范流程] → [任何外部/上游 AI 出的方案，落地到本仓前必须用本仓 AGENTS.md「改 skill 必走 build_pipeline_skill、待办真相源是 tasks/todo.md」复核，不直接执行]
- [跨库待办双真相源] → [Notes 侧 cgi_pipeline.md 和本仓 tasks/todo.md 各维护一份 CGI 待办，易漂移] → [按 AGENTS.md「待办真相源是 todo.md」把细则迁回本仓，Notes 侧只留指针] → [CGI 项目待办的唯一真相源是 tasks/todo.md；外部摘要层只能留指针，不复制细则]

# API Help: maya.rig.maya_get_hierarchy

- 作用与场景：maya_get_hierarchy
- 用法：`execute_api(maya.rig.maya_get_hierarchy, params={...})`
- DCC：`maya`；层级：`write`；执行：`background / foreground`

## 参数
- 无专属参数。

## 输出
- maya_get_hierarchy

此文件是渐进式阅读入口；机器契约以同目录 `api.yaml` 为准。

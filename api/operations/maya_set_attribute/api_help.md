# API Help: maya.asset.maya_set_attribute

- 作用与场景：maya_set_attribute
- 用法：`execute_api(maya.asset.maya_set_attribute, params={...})`
- DCC：`maya`；层级：`write`；执行：`background / foreground`

## 参数
- 无专属参数。

## 输出
- maya_set_attribute

此文件是渐进式阅读入口；机器契约以同目录 `api.yaml` 为准。

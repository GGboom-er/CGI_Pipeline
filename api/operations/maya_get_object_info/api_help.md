# API Help: maya.asset.maya_get_object_info

- 作用与场景：maya_get_object_info
- 用法：`execute_api(maya.asset.maya_get_object_info, params={...})`
- DCC：`maya`；层级：`write`；执行：`background / foreground`

## 参数
- 无专属参数。

## 输出
- maya_get_object_info

此文件是渐进式阅读入口；机器契约以同目录 `api.yaml` 为准。

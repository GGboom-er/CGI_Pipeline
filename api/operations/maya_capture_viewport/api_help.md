# API Help: maya.asset.maya_capture_viewport

- 作用与场景：获取 Maya 当前活动视口的截图，并保存为 PNG。
- 用法：`execute_api(maya.asset.maya_capture_viewport, params={...})`
- DCC：`maya`；层级：`read`；执行：`background / foreground`

## 参数
- 无专属参数。

## 输出
- 获取 Maya 当前活动视口的截图，并保存为 PNG。

此文件是渐进式阅读入口；机器契约以同目录 `api.yaml` 为准。
